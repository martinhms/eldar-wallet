<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".ui.EldarWalletApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_eldar"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_eldar_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EldarWallet"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.addcard.AddDigitalCardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.main.MainActivity"
            android:exported="true"></activity>
    </application>

</manifest>